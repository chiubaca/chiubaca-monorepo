---
import { noteTypes } from "@shared/types";
import BaseLayout from "../layouts/BaseLayout.astro";

const noteTypeTitles = Object.values(noteTypes.enum).map((noteType) => {
  return noteType;
});
---

<BaseLayout>
  <main class="">
    <ul class="flex flex-col">
      {
        noteTypeTitles.map((title) => {
          switch (title) {
            case "fleeting-notes":
              return (
                <a class="link link-hover" href={title}>
                  <li>📝 All Fleeting Notes</li>
                </a>
              );
            case "literature-notes":
              return (
                <a class="link link-hover" href={title}>
                  <li>📚 All Literature Notes</li>
                </a>
              );
            case "permanent-notes":
              return (
                <a class="link link-hover" href={title}>
                  <li>🧠 Permanent Notes</li>
                </a>
              );
            case "index-notes":
              return (
                <a class="link link-hover" href={title}>
                  <li>🔢 Index Notes</li>
                </a>
              );

            default:
              break;
          }
        })
      }

      <a class="link link-hover" href="/tags">
        <li>🔖 Tags</li>
      </a>
    </ul>
  </main>
</BaseLayout>
